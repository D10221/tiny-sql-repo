name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2    
    - name: install and build and test
      run: |
        yarn,
        yarn build,
        yarn test
